export interface BrokerAllocate {
    symbol: string;
    quantity: number;
}

export interface ProportionLocate {
    machine: string;
    symbol: string;
    quantity: number;
    proportion: number;
}